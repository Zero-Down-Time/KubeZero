apiVersion: v1
kind: Service
metadata:
  name: eventhub
  labels:
    app: eventhub
    {{- include "kubezero-lib.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
  selector:
    app: eventhub
