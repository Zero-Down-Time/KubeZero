neo4j:
  enabled: false

  disableLookups: true

  neo4j:
    name: test-db

    password: secret
    passwordFromSecret: "neo4j-admin"

  volumes:
    data:
      mode: defaultStorageClass

  services:
    neo4j:
      enabled: false

  serviceMonitor:
    enabled: false
